import Typography from '@/components/primitives/Typography';
import { GridItem } from '@/components/ui/ui-layout';
import { Grid } from 'lucide-react';
import React from 'react'

const ApprBreakDown = () => {
  return (
    <GridItem>
        <Typography variant='h5' weight='semibold'>Approval Breakdown</Typography>
    </GridItem>
  )
}

export default ApprBreakDown;
